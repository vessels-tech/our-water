import 'mocha'
import * as assert from 'assert';

describe.skip('GetFoiResponse Unit Tests', function () {
  it('Deserialises the feature of interest response', () => {
    //Arrange
    const request = {};
    const expected = '';

    //Act
    const response = '';

    //Assert
    assert.equal(response, expected);
  });
});